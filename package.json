{
  "name": "onequestionevery.day",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8080 --host=127.0.0.1 --no-browser --watch=index.html --ignore=node_modules"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
